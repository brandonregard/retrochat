RetroChat for Windows AMD64
===========================

Run RETROCHAT.MSI to install the native 64-bit client, server, and bundled
Tcl/Tk runtime. This package targets 64-bit Windows 10 or newer. It does not
replace the separate Windows 95/98 i386 package.
