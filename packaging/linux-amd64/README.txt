RetroChat for Debian Linux AMD64
================================

This ISO targets Debian 12-compatible AMD64 systems. Open a terminal in this
disc and run:

    sh INSTALL.SH

The installer uses apt to install the small system prerequisites required by
the bundled Tcl/Tk 8.6 AMD64 package, then installs RetroChat. Internet access
may be required for those prerequisites.
